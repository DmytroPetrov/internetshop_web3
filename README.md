# internetshop_web3

## Backend
run next commands

`cd backend`

`sudo apt install python3-pip`

`pip install -r `

`pip install -r requirements.txt`

`cd shop_backend/`

`python manage.py runserver`

``
